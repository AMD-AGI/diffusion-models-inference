# create-pr

Creates a pull request in the repository associated with the current checkout.
Populates the repository PR template from an optional validated issue, branch
commits, and the diff against the target branch.
