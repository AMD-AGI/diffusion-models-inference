# create-release

Creates a GitHub release in the repository associated with the current checkout.
Supports GitHub-generated notes, user-provided Markdown, or output from the
`create-release-notes` skill.
