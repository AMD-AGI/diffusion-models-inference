# get-benchmark-results

Fetches median benchmark latencies from completed `build-and-benchmark.yml` runs
in the repository associated with the current checkout. Supports explicit
two-run comparisons and optional wall-clock summaries when present in logs.
