# trigger-build-and-benchmark

Maintainer-only skill that dispatches the current `build-and-benchmark.yml`
workflow in the repository associated with the checkout. Supports source and
prebuilt image flows, benchmark-only runs, and MIOpen tuning modes.
