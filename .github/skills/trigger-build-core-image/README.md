# trigger-build-core-image

Maintainer-only skill that dispatches `build-core-image.yml` in the repository
associated with the current checkout. The workflow builds and pushes core and
untuned images.
