diff --git a/xfuser/model_executor/models/runner_models/ltx.py b/xfuser/model_executor/models/runner_models/ltx.py
index 2531cc1..940830d 100644
--- a/xfuser/model_executor/models/runner_models/ltx.py
+++ b/xfuser/model_executor/models/runner_models/ltx.py
@@ -1,4 +1,5 @@
 import copy
+import gc
 from types import SimpleNamespace
 from typing import ClassVar
 
@@ -605,6 +606,24 @@ class _xFuserLTX25VideoModelBase(xFuserModel):
                     "Using guidance_scale=1.0. Other guidance scale values are not supported with this model."
                 )
 
+    def run(self, input_args: dict):
+        gc_was_enabled = gc.isenabled()
+        gc.disable()
+        try:
+            return super().run(input_args)
+        finally:
+            if gc_was_enabled:
+                gc.enable()
+
+    def profile(self, input_args: dict):
+        gc_was_enabled = gc.isenabled()
+        gc.disable()
+        try:
+            return super().profile(input_args)
+        finally:
+            if gc_was_enabled:
+                gc.enable()
+
     def _run_pipe(self, input_args: dict) -> DiffusionOutput:
         from diffusers.pipelines.ltx2.utils import (
             DISTILLED_SIGMA_VALUES,
